import React from 'react'

export const User = ({users}) => {
    return (
        <div>
            {users.map((user) => (
                return (user.username)
            )}
        </div>
    )
}
